name: video_player_for_web_integration_tests
publish_to: none

environment:
  sdk: ^3.2.2
  flutter: ">=3.16.2"

dependencies:
  flutter:
    sdk: flutter
  video_player_platform_interface: ^6.1.0
  video_player_web:
    path: ../
  web: ^0.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
